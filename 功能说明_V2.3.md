# 好宅助手 V2.3 功能说明

## 功能概述

本次更新实现了两个关键功能改进：

### 1. 户型图朝向与罗盘绑定

**问题**：用户用罗盘选择"坐东朝西"，但AI识别后朝向仍显示南向，造成混淆。

**解决方案**：

1. **坐向/朝向清晰标注**
   - 在户型图调整页面顶部显示"坐X朝Y"标识
   - 坐向 = 朝向 + 180°（房屋坐向与大门朝向相反）
   - 例如：大门朝南 → 房屋坐北

2. **户型图旋转显示**
   - 根据用户选择的罗盘朝向角度自动旋转户型图
   - 使用 CSS `transform: rotate()` 实现
   - 让用户直观看到实际方位

3. **罗盘图标叠加**
   - 在户型图右上角显示迷你罗盘指示器
   - 清晰标识北(N)方向
   - 罗盘跟随旋转角度同步显示

4. **方向标签**
   - 在户型图边缘标注上下左右方向
   - 帮助用户理解方位对应关系

### 2. 九宫格可视化与微调

**需求**：

1. **AI识别后展示九宫格**
   - 户型图识别通过后自动显示九宫格叠加效果
   - 九宫格对应八卦宫位（坎、坤、震、巽、中、乾、兑、艮、离）
   - 用颜色区分吉凶（绿色=吉，灰色=平，红色=凶）

2. **交互微调功能**
   - **拖拽移动**：拖动九宫格中心控制点可移动九宫格位置
   - **大小调整**：滑块控制九宫格缩放（50%-150%）
   - **角度调整**：滑块控制九宫格旋转（-45°到+45°）
   - **方向选择**：点击按钮切换九宫格上方朝向（北/东北/东/东南/南/西南/西/西北）

3. **数据使用逻辑**
   - 用户不调整 → 使用AI识别数据
   - 用户调整了 → 使用调整后的数据传递给后续分析

## 用户流程

```
罗盘测量 → 户型图上传 → AI识别验证
                            ↓
                     九宫格微调界面
                    ↙    ↓    ↘
               拖拽移动  滑块调整  方向选择
                    ↘    ↓    ↙
                     确认/跳过
                            ↓
                     装修评估
```

## 技术实现

### 核心文件

1. **FloorPlanAdjuster.jsx** (新建)
   - 户型图旋转与九宫格微调组件
   - 处理拖拽、缩放、旋转交互

2. **FloorPlanUpload.jsx** (修改)
   - 新增调整状态 `adjusting`
   - 集成 FloorPlanAdjuster 组件
   - 传递调整数据到后续步骤

3. **FloorPlanHeatmap.jsx** (修改)
   - 支持使用调整后的九宫格参数
   - 优先使用用户自定义的朝向

4. **App.css** (修改)
   - 新增 V2.3 相关样式

### 状态数据流

```javascript
// 调整完成后的数据格式
{
  preview: "base64图片",
  validation: { /* AI验证结果 */ },
  rooms: [ /* 识别到的房间 */ ],
  floorPlanInfo: { /* 户型信息 */ },
  adjustedData: {
    gridOffset: { x: 0, y: 0 },    // 九宫格偏移
    gridScale: 1,                   // 缩放比例
    gridAngle: 0,                   // 旋转角度
    gridTopDirection: '北',         // 上方朝向
    rotation: 180,                  // 户型图旋转角度
    facing: '南',                   // 朝向
    zuoXiang: '坐北朝南',           // 坐向标注
    gridOrder: [['巽','离','坤'],...] // 九宫格卦象布局
  }
}
```

## 交互细节

### 调整控制面板

| 控制项 | 功能 | 范围 |
|--------|------|------|
| 大小 | 缩放九宫格 | 50% - 150% |
| 角度 | 旋转九宫格 | -45° - +45° |
| 上方朝向 | 切换九宫格朝向 | 北/东北/东/东南/南/西南/西/西北 |
| 显示开关 | 显示/隐藏九宫格 | 开启/关闭 |

### 快捷操作

- **重置按钮**：一键恢复所有调整为默认值
- **跳过微调**：直接使用AI识别数据继续
- **拖拽中心点**：移动九宫格到图片任意位置

## 样式特点

保持白色赛博国风品牌调性：
- 铜金色主色调 (`#a67c3d`)
- 毛玻璃背景效果
- 流畅的过渡动画
- 响应式设计适配移动端

## 验收标准

✅ 用户选择坐向后，户型图按正确角度旋转显示
✅ 罗盘标识清晰，方位标注明确
✅ 九宫格可可视化调整
✅ 调整后的数据能正确传递到后续分析步骤
✅ UI风格与现有白色赛博国风保持一致
